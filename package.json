{
  "name": "shingo-camera-platform",
  "version": "0.1.0",
  "private": true,
  "description": "shingo-camera Platform 共通基盤 (WORK-001)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "pretest": "node test/build-test-bundle.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260804.1",
    "@supabase/supabase-js": "^2.112.1",
    "esbuild": "^0.28.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.119.0"
  },
  "dependencies": {
    "jose": "^6.2.8",
    "stripe": "^22.4.0"
  }
}
